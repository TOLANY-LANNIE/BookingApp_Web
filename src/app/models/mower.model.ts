export interface Mower {
    id: string;             // Unique identifier for the mower
    name: string;          // Name of the mower machine
    operatorId: string;   
    imageUrl?:string // Operator ID associated with the mower
  }